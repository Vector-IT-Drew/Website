# Flask app — force Python install (root package.json was making Nixpacks Node-only)
providers = ["python"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT app:app"
