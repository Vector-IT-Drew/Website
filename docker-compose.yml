services:
  web:
    build: .
    ports:
      - "5005:8080"
    environment:
      PORT: "8080"
      SESSION_SECRET: "local-dev-secret"
    # Create .env from .env.example if you need secrets locally
    # env_file:
    #   - .env
